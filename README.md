Docker Flask Gunicorn Sample
============================

Sample Docker setup using Flask, Gunicorn, and Nginx.

#### Build

    $ docker-compose build

#### Run

With nginx and gunicorn:

    $ docker-compose up -d --force-recreate
